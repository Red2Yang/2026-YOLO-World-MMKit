# data_text_json folder

This folder is for `class_text_path` configs, which are imported to provide classification info of datasets for `train_dataset` and `val_dataset`.

The format is:

```
[["person"], ["bicycle"]]
```